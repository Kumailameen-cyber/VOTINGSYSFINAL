<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page - Online Voting System</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <style>
        @@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: #ffffff;
            color: #1a1a1a;
            overflow-x: hidden;
        }

        /* Animated gradient background */
        .hero-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #f8faf9 0%, #ffffff 50%, #f0f7f4 100%);
            z-index: -2;
        }

        /* Floating particles effect */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            opacity: 0.15;
        }

        .particle {
            position: absolute;
            border-radius: 50%;
            background: linear-gradient(135deg, #228B22, #006400);
            animation: float 20s infinite;
        }

            .particle:nth-child(1) {
                width: 80px;
                height: 80px;
                left: 10%;
                animation-delay: 0s;
            }

            .particle:nth-child(2) {
                width: 60px;
                height: 60px;
                left: 80%;
                animation-delay: 4s;
            }

            .particle:nth-child(3) {
                width: 40px;
                height: 40px;
                left: 50%;
                animation-delay: 8s;
            }

            .particle:nth-child(4) {
                width: 100px;
                height: 100px;
                left: 30%;
                animation-delay: 12s;
            }

            .particle:nth-child(5) {
                width: 50px;
                height: 50px;
                left: 70%;
                animation-delay: 16s;
            }

       @@keyframes float {
            0%, 100%

        {
            transform: translateY(0) translateX(0) scale(1);
            opacity: 0.3;
        }

        25% {
            transform: translateY(-100px) translateX(50px) scale(1.1);
            opacity: 0.5;
        }

        50% {
            transform: translateY(-200px) translateX(-50px) scale(0.9);
            opacity: 0.3;
        }

        75% {
            transform: translateY(-100px) translateX(-100px) scale(1.05);
            opacity: 0.4;
        }

        }

        .container {
            position: relative;
            z-index: 1;
        }

        /* Hero card with glassmorphism */
        .hero-card {
            background: linear-gradient(135deg, #228B22 0%, #006400 100%);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 32px;
            padding: 4rem 3rem;
            box-shadow: 0 20px 60px rgba(34, 139, 34, 0.3);
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            animation: slideDown 0.8s ease-out;
            overflow: hidden;
            position: relative;
            color: #ffffff;
        }

            .hero-card::before {
                content: '';
                position: absolute;
                top: 0;
                left: -100%;
                width: 100%;
                height: 100%;
                background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
                transition: left 0.8s;
            }

            .hero-card:hover::before {
                left: 100%;
            }

            .hero-card:hover {
                transform: translateY(-10px);
                box-shadow: 0 30px 80px rgba(34, 139, 34, 0.4);
                border-color: rgba(255, 255, 255, 0.4);
            }

        @@keyframes slideDown {
            from

        {
            opacity: 0;
            transform: translateY(-50px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        .hero-logo {
            height: 120px;
            width: auto;
            filter: drop-shadow(0 10px 30px rgba(34, 197, 94, 0.5));
            animation: pulse 3s ease-in-out infinite;
        }

        @@keyframes pulse {
            0%, 100%

        {
            transform: scale(1);
        }

        50% {
            transform: scale(1.05);
        }

        }

        .hero-title {
            font-size: 2.5rem;
            font-weight: 800;
            color: #ffffff;
            margin: 1.5rem 0 1rem;
            animation: fadeIn 1s ease-out 0.3s backwards;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
        }

        .hero-subtitle {
            font-size: 1.25rem;
            color: rgba(255, 255, 255, 0.95);
            font-weight: 400;
            animation: fadeIn 1s ease-out 0.6s backwards;
        }

        @@keyframes fadeIn {
            from

        {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        /* Feature cards */
        .feature-card {
            background: #ffffff;
            border: 3px solid #228B22;
            border-radius: 24px;
            padding: 2.5rem 2rem;
            height: 100%;
            transition: all 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
            cursor: pointer;
            position: relative;
            overflow: hidden;
            box-shadow: 0 8px 20px rgba(34, 139, 34, 0.2);
            display: block;
            text-decoration: none;
        }

            .feature-card::before {
                content: '';
                position: absolute;
                bottom: 0;
                left: 0;
                width: 100%;
                height: 0;
                background: linear-gradient(135deg, #228B22 0%, #006400 100%);
                transition: height 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
                z-index: 0;
            }

            .feature-card::after {
                content: '';
                position: absolute;
                top: -3px;
                left: -3px;
                right: -3px;
                bottom: -3px;
                background: linear-gradient(135deg, #228B22, #006400);
                border-radius: 24px;
                opacity: 0;
                z-index: -1;
                transition: opacity 0.6s ease;
            }

            .feature-card:hover::before {
                height: 100%;
            }

            .feature-card:hover::after {
                opacity: 1;
            }

            .feature-card:hover {
                transform: translateY(-25px) scale(1.03);
                border-color: transparent;
                box-shadow: 0 35px 70px rgba(34, 139, 34, 0.4), 0 15px 30px rgba(0, 100, 0, 0.3);
            }

        .card-content {
            position: relative;
            z-index: 2;
            transition: all 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
        }

        .feature-card:hover .card-content {
            transform: scale(1.05);
        }

        .col-md-4:nth-child(1) .feature-card {
            animation: slideUp 0.8s ease-out 0.2s backwards;
        }

        .col-md-4:nth-child(2) .feature-card {
            animation: slideUp 0.8s ease-out 0.4s backwards;
        }

        .col-md-4:nth-child(3) .feature-card {
            animation: slideUp 0.8s ease-out 0.6s backwards;
        }

        @@keyframes slideUp {
            from

        {
            opacity: 0;
            transform: translateY(50px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        .icon-wrapper {
            width: 90px;
            height: 90px;
            margin: 0 auto 1.5rem;
            background: linear-gradient(135deg, #228B22 0%, #006400 100%);
            border-radius: 22px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            transition: all 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
            overflow: hidden;
            box-shadow: 0 8px 20px rgba(34, 139, 34, 0.4);
        }

            .icon-wrapper::before {
                content: '';
                position: absolute;
                width: 100%;
                height: 100%;
                background: #ffffff;
                opacity: 0;
                transition: opacity 0.6s ease;
            }

        .feature-card:hover .icon-wrapper {
            transform: scale(1.4);
            box-shadow: 0 15px 40px rgba(34, 139, 34, 0.6);
        }

            .feature-card:hover .icon-wrapper::before {
                opacity: 1;
            }

        .icon-wrapper i {
            font-size: 2.8rem;
            color: #ffffff;
            transition: all 0.6s ease;
            position: relative;
            z-index: 1;
            filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.3));
        }

        .feature-card:hover .icon-wrapper i {
            color: #228B22;
            transform: scale(1.15);
            filter: drop-shadow(0 6px 12px rgba(34, 139, 34, 0.5));
        }

        .feature-title {
            font-size: 1.6rem;
            font-weight: 800;
            margin-bottom: 1rem;
            color: #1a1a1a;
            transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
            position: relative;
        }

        .feature-card:hover .feature-title {
            color: #ffffff;
            transform: translateY(-8px);
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.4);
        }

        .feature-text {
            color: #374151;
            line-height: 1.8;
            font-size: 1.05rem;
            transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) 0.1s;
            font-weight: 500;
        }

        .feature-card:hover .feature-text {
            color: #ffffff;
            transform: translateY(-5px);
            text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
        }

        /* Glow effect on hover */
        .glow {
            display: none;
        }

        /* Responsive */
        @@media (max-width: 768px) {
            .hero-title

        {
            font-size: 1.8rem;
        }

        .hero-subtitle {
            font-size: 1rem;
        }

        .hero-card {
            padding: 2.5rem 1.5rem;
        }

        }
    </style>
</head>
<body>
    <div class="hero-bg"></div>
    <div class="particles">
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
    </div>

    <div class="container mt-5 py-5">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="hero-card text-center">
                    <img src="/images/logo.png" class="hero-logo" alt="Logo" />
                    <h1 class="hero-title">
                        Welcome to the Online Voting System
                    </h1>
                    <p class="hero-subtitle">
                        A secure and transparent platform designed to simplify digital voting and result tracking.
                    </p>
                </div>
            </div>
        </div>

        <div class="row mt-5 g-4">
            <div class="col-md-4">
                <a asp-action="About" class="feature-card">
                    <div class="card-content">
                        <div class="icon-wrapper">
                            <i class="fas fa-info-circle"></i>
                        </div>
                        <h5 class="feature-title">About the System</h5>
                        <p class="feature-text">
                            Understand how the system maintains election integrity through secure and reliable processes.
                        </p>
                    </div>
                </a>
            </div>

            <div class="col-md-4">
                <a asp-action="Elections" class="feature-card">
                    <div class="card-content">
                        <div class="icon-wrapper">
                            <i class="fas fa-poll-h"></i>
                        </div>
                        <h5 class="feature-title">Active Elections</h5>
                        <p class="feature-text">
                            View ongoing elections and cast your vote once your identity is verified.
                        </p>
                    </div>
                </a>
            </div>

            <div class="col-md-4">
                <a asp-controller="PublicResult" asp-action="Index" class="feature-card">
                    <div class="card-content">
                        <div class="icon-wrapper">
                            <i class="fas fa-chart-bar"></i>
                        </div>
                        <h5 class="feature-title">View Results</h5>
                        <p class="feature-text">
                            Access accurate and transparent election results after official publication.
                        </p>
                    </div>
                </a>
            </div>
        </div>
    </div>
</body>
</html>